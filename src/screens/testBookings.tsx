import React from "react";
import { StyleSheet, Text, View } from "react-native";

const TestBookings = () => {
  return (
    <View>
      <Text>Test Bookings</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default TestBookings;

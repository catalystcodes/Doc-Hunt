import React from "react";
import { StyleSheet, Text, View } from "react-native";

const BookMark = () => {
  return (
    <View>
      <Text style={{ marginTop: 80, textAlign: "center" }}>BookMark Page</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default BookMark;

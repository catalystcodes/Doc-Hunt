import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Comment = () => {
  return (
    <View>
      <Text style={{ marginTop: 80, textAlign: "center" }}>Comment Page</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Comment;
